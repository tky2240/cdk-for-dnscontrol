import { Construct } from "constructs";
import { DnscontrolIgnore } from "../management/ignore";
export declare function IGNORE(scope: Construct, labelSpec: string, typeSpec?: string, targetSpec?: string): DnscontrolIgnore;
